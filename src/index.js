import React from 'react';
import ReactDOM from 'react-dom';
import './frontend/styles/index.css'; // Optional, falls vorhanden
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
